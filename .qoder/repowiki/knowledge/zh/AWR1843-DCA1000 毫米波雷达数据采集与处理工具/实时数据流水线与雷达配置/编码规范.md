- 对外暴露的 I/O 路径通过 `#if 1` / `#if 0` 宏开关在同一循环内切换（解析后写入 vs 原始 ADC 写入），避免维护两套循环逻辑。
- 共享状态采用原子标志 + 互斥锁的组合：`processing_` 控制生命周期，`bufferMutex_` 保护累积缓冲，`fileMutex_` 保护文件写入，三者职责单一且成对出现。
- 批处理采用“计数器阈值 + 到达即触发”的模式：`frameCounter_` 累加并在 `>= FRAME_BATCH_SIZE` 时执行写入并重置，随后置 `processing_=false` 退出循环。