- 对外解析接口统一采用bool返回值+输出参数引用模式，失败路径通过std::cout/std::cerr打印错误并返回false。
- 所有需要修改成员变量的公共方法在入口处用std::unique_lock<std::mutex>加锁，保证线程安全。
- 输出容器一律先resize再写入，注释明确区分reserve与resize语义，防止越界。
- 原始字节流通过reinterpret_cast<const int16_t*>直接解释为int16_t序列，按固定步长(chirp→rx→sample)索引访问，避免中间缓冲。